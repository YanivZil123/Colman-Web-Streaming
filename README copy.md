# Disney+ Demo App (Course Project)
Vanilla HTML/CSS (no front-end frameworks) + Node/Express.

## Run
```bash
npm install
npm run dev
```
Then open http://localhost:3000

Default admin: admin@example.com / admin123


This build keeps all data in memory (mock store). Restart clears data.
